---
import BaseLayout from '../layouts/BaseLayout.astro';
import HeroHeader from '../components/HeroHeader.astro';
import LegalTabs from '../components/LegalTabs.astro';
import { politiqueConfidentialiteContent } from '../data/legalData';

const { title, metaTitle, metaDescription, intro, sections } = politiqueConfidentialiteContent;
---

<BaseLayout title={metaTitle} description={metaDescription}>
  
  <!-- HERO HEADER REUSABLE -->
  <HeroHeader
    badge="Protection des Données & RGPD"
    title={title}
    description={intro}
    breadcrumbs={[{ label: title, href: '/politique-confidentialite' }]}
  />

  <!-- LEGAL CONTENT -->
  <section class="py-12 sm:py-16 max-w-content mx-auto px-4 sm:px-6 lg:px-8">
    <LegalTabs activeTab="confidentialite" />

    <div class="card-luxury p-6 sm:p-10 md:p-12 bg-white border border-gray-100 mt-8 space-y-10 prose-aziman shadow-sm">
      {sections.map((sec) => (
        <div class="space-y-3">
          <h2 class="font-serif font-bold text-xl sm:text-2xl text-brand-dark border-b border-gold-200/60 pb-2.5">
            {sec.title}
          </h2>
          <div class="text-xs sm:text-sm text-gray-700 leading-relaxed whitespace-pre-line font-sans">
            {sec.content}
          </div>
        </div>
      ))}
    </div>
  </section>

</BaseLayout>
